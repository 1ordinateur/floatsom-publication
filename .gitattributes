# Preserve binary publication artifacts and the byte-exact Zenodo deposit.
*.pdf binary
*.zip binary

# SHA256SUMS is calculated over LF-normalized deposit files. Keep those bytes
# stable across Windows and Unix checkouts; the binary rules above take priority.
zenodo/floatsom_extended_supplementary/** text eol=lf
zenodo/floatsom_extended_supplementary/**/*.pdf binary
