# Preserve binary publication artifacts and the extended supplementaries archive.
*.pdf binary
*.zip binary

# SHA256SUMS is calculated over LF-normalized archive files. Keep those bytes
# stable across Windows and Unix checkouts; the binary rules above take priority.
extended_supplementaries/** text eol=lf
extended_supplementaries/**/*.pdf binary
