# Publication benchmark tests

These tests cover benchmark orchestration, analysis and manuscript asset wiring.
Runtime tests live in the standalone FloatSOM repository. Install both packages
before testing. CUDA and Ray tests require the corresponding hardware; the README
and CI workflow list the CPU-only subset.
